# ===========================================
# Anti2API - Environment Configuration
# ===========================================

# ----- Authentication (Required) -----
# If not configured, system will auto-generate random credentials on startup
# API_KEY=your-api-key
# ADMIN_USERNAME=your-username
# ADMIN_PASSWORD=your-password
# JWT_SECRET=your-jwt-secret

# ----- Network (Optional) -----
# Proxy server for outgoing requests
# PROXY=http://127.0.0.1:7890

# ----- AI Configuration (Optional) -----
# Default system instruction for AI responses
SYSTEM_INSTRUCTION=You are a helpful AI assistant.

# Base URL for serving generated images (leave empty for relative paths)
# IMAGE_BASE_URL=http://your-domain.com
